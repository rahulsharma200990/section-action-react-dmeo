name: GetOutput Workflow 
on: workflow_dispatch
jobs:
    infojob:
        runs-on: ubuntu-latest
        steps:
            - name: Output Github Context
              run: echo "${{ toJson(github)}}"  